spring:
  datasource:
    password: srivatsan
    url: jdbc:mysql://localhost:3306/test
    username: root
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  

  batch:
    jdbc:
      initialize-schema: always